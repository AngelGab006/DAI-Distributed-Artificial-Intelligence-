# How to run

need Python install, and extension needed to run jupyter notebook on your prefer IDE
In this case Python 3.13.9 is being use

- create a virtual environment:

```
python -m venv ./venv
```

- Install the needed dependencies

```
pip install numpy pandas networkx matplotlib
```